parameters:
  cors.config:
    enabled: true
    allowedHeaders: ['x-csrf-token', 'authorization', 'contenttype', 'accept', 'origin', 'x-requested-with']
    allowedMethods: ['GET']
    allowedOrigins: ['*']
    exposedHeaders: true
    maxAge: false
    supportsCredentials: false

{#
/**
 * @file
 * Theme override for the sf-phone-numbers field on a sf-person full node.
 *
 * Available variables:
 * - Look in the field.html.twig file for available variables.
 *
 * @see template_preprocess_field()
 */
#}
{% for item in items %}
  <li class="icon icon--phone">{{ item.content }}</li>
{% endfor %}
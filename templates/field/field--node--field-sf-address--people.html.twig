{#
/**
 * @file
 * Theme override for the sf-address field on a sf-person full node.
 *
 * Available variables:
 * - Look in the field.html.twig file for available variables.
 *
 * @see template_preprocess_field()
 */
#}
{% for item in items %}
    <span class="icon icon--map">{{ item.content }}</span>
{% endfor %}